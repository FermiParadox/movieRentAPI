# Create an "IGNORE_GIT_SECRETS.py" file and place MONGO_DB_LINK there
# since that filename is ignored on git commits.

# Or edit this (WARNING: don't accidentally push it on GitHub)
MONGO_DB_LINK = 'not_real_link'
